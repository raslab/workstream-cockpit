{
  "name": "workstream-cockpit",
  "version": "1.0.0",
  "description": "Personal productivity tool for tracking active workstreams with status history",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspaces",
    "build:backend": "npm run build --workspace=backend",
    "build:frontend": "npm run build --workspace=frontend",
    "test": "npm run test --workspaces --if-present",
    "test:backend": "npm run test --workspace=backend",
    "test:frontend": "npm run test --workspace=frontend",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "docker": "VITE_GIT_BRANCH=$(git rev-parse --abbrev-ref HEAD) VITE_GIT_COMMIT=$(git rev-parse HEAD) VITE_GIT_COMMIT_DATE=$(git show -s --format=%cI HEAD) VITE_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) docker compose up --build -d",
    "docker:up": "docker-compose up",
    "docker:down": "docker-compose down",
    "docker:build": "VITE_GIT_BRANCH=$(git rev-parse --abbrev-ref HEAD) VITE_GIT_COMMIT=$(git rev-parse HEAD) VITE_GIT_COMMIT_DATE=$(git show -s --format=%cI HEAD) VITE_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ) docker compose build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "overrides": {
    "uuid": "11.1.1",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "@types/react": "18.3.31",
    "@types/react-dom": "18.3.7"
  }
}
